在常见的Linux扩展文件系统中(如ext2、ext3、ext4等)，可以借助某种文件属性将文件设置 为不可修改(immutable)。
一旦设置，任何用户(包括超级用户)都不能删除该文件，除非其不可修改的属性被移除

chattr能够将文件设置为不可修改
(1) 使用下列命令将一个文件设置为不可修改
    chattr +i file
如果需要使文件恢复可写状态，移除不可修改属性即可
chattr -i file