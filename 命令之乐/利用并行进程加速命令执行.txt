在过去的几年中，计算能力有了大幅度的攀升。
但这并不是仅仅因为处理器有了更高的时钟频率，而是因为多核的出现。
对于用户而言，这意味着单个物理处理器中包含了多个逻辑处理器。
除非软件能够善加利用多核，否则它们毫无用武之地。
如果你有一个需要进行大量运算的程序，仅运行在其中一个核心上，那么其他的核心都会被闲置。
如果想提高速度，软件必须留意并充分利用多核

以md5sum为例,涉及到计算，这事cpu密集型命令
多个文件需要生成校验和，我们可以使用下面的脚本来运行md5sum的多个实例
bash generate_chechsums.sh 具体解释看脚本
