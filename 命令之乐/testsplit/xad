			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
6.一个规则里，单项对比，整单比对，整单求和，整单平均可以共存。



---- 原型问题反馈
	新增数据对象页面
		尽量延用原来的老系统样式
		字段数据类型，int ，string，date，float
		字段列表，添加是否为主键
	业务对象版本管理功能去掉

	应用管理 ： 添加生效时间、失效时间 ，app密钥
			   删除判断

druid sqlparse


1、场景中的参数管理，应用系统要加场景ID，场景列表引擎平台提供接口（应用系统待办）；
2、权限管理放到应用下做；
3、审批管理放到应用下做；
4、规则树右键功能应用根据自己的需求在扩充，例如审批等，规则引擎方要提供按钮接口（应用系统待办）；
5、确定同一个树，发布的只有一个（引擎实现）；
6、规则树接口需要应用提供规则树id和版本号id，可以用哪些参数表的标签ID（应用系统，引擎要注意接收）；
8、条件配置要弹出最大化页配置；
9、规则树加停止按钮，并提供接口（引擎）；
10、规则树编号由应用系统生产，新增时传给规则引擎，规则引擎也要存一份；
12、指令谁开发，规则引擎平台开发没有授权，应用开发需要应用提供接口，所有指令建议都在一个树上维护（待定，庆铎回来确定）；
13、规则里的扩展信息在规则引擎平台保存，推送给应用平台；
14、参数表应用与引擎共用；
15、应用传过来的数据格式要订标准，传入、传出都用消息队列实现；（刘鑫和晓伟碰一下）
16、报关单舱单融合为一个场景引擎平台出方案（刘鑫和晓伟碰一下）；
17、正式环境结论数据结构定义待定，推送消息队列，推送具体内容（规则编号，规则树编号，场景ID，应用提供唯一对象ID）待定（规则引擎待定，然后给应用平台在讨论）；
18、规则校验（规则语法校验），批量测试要做；
19、测试结果引擎平台提供页面，引擎平台注意提供是body页；
20、规则里的生效与失效保留，失效定义为本节点规则及子节点规则；
21、参数表数据命中每日上限，总上限统计及参数数据自动终止；（应用待定，引擎暂不实现）



检验检疫比对类型：
1.       单项比对：多条数据中有一条满足校验条件，则比对成功，比对只在一个报关单范围内进行。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中有一个比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
2.       整单比对：多条数据中所有数据都满足校验条件，比对只在一个报关单范围内进行。则比对成功。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1,2,3,4分别比对a条件,1,2,3,4中全部比对结果为true，则此比对就为true，依次类推执行b,c条件比对，最终在执行表达式计算，返回最终结论；
3.       整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行avg,得出的结果跟条件进行比对；
4.       整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对，例如例如一个报关单下有1,2,3,4四个商品项，分别对1,2,3,4中某个字段进行sum,得出的结果跟条件进行比对；
5.       单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，最终所有比对结果为ture，就算true；
6.       单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中。例如一个报关单下有1,2,3,4四个商品项，有a,b,c三个条件和一个最终表达式a&&b||c，比对项只限制在商品项目（表体）比对，1分别比对a,b,c条件，然后在参与表达式计算，最终输出此次比对结果，依次类推执行2,3,4比对，然后最终只要有一个比对结果为ture，就算true；


规则引擎平台接口


1.参数表相关接口
	表结构
	增删改查
	数据同步接口 :同步方式、存储方式?
	参数表绑定场景 === 场景查询接口
	===== 场景的维护 增删改查
2.规则相关接口
	场景的维护 基本的增删改查 ,查询条件 ,按应用区分
	规则树维护 基本的增删改查 ，对外的条件查询
	规则扩展属性页面接口
		应用在创建规则之前要先填写一些外围扩展属性,
		在调用创建之前回掉应用扩展属性页面,通过配置指定回掉页面
	规则测试接口
		基于规则树的调试
		基于规则树或者森林的仿真、批量测试




--- 业务比对类型

1.单项比对：多条数据中有一条满足校验条件，则比对成功
2.整单比对：多条数据中所有数据都满足校验条件，则比对成功
3.整单平均：对多条数据中，针对某一个字段对所有数据进行求平均值，该平均值参与比对
4.整单求和：对多条数据中，针对某一个字段对所有数据进行求和，该求和数值参与比对

拿集合去碰撞每一个条件	



5.单项整单:多项货物同时需要比对在同一数据表中的多个数据项，且都要同时符合条件。
6.单项复合：在同一个数据表中有多个数据项需要参与多个比对条件校验，只要有一个货物满足多个条件即命中



和原系统开发方确认的内容：
1.单项对比，整单比对，整单求和，整单平均 均是将集合（比如：多个商品项）分别对碰每一个条件，最后再将每个条件的布尔值参与表达式的计算。
2.单项整单，单项复合是以每一个数据行去碰撞所有的条件。
3.只有单项整单，单项复合才会产生筛选的结果。而单项对比，整单比对，整单求和，整单平均只会产生true或false。
4.一个规则里，单项整单不能同其他比对类型共存。并且多个单项整单条件均是对同一个数据表配置条件。
5.一个规则里，单项复合不能同其他比对类型共存。并且多个单项复合条件均是对同一个数据表配置条件。
